import { ServicesListContainer } from '@/features/services/containers/ServicesListContainer';

export const metadata = {
  title: 'Services | Admin',
};

export default function ServicesPage() {
  return <ServicesListContainer />;
}
